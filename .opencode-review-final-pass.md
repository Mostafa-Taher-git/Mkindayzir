# OpenCode Final Pass — finish everything that is still missing

Project: /media/dell/New Volume/Projects/OpsDesk
Server: ALREADY running at http://127.0.0.1:5000 — do NOT start your own server.
Model to use: opencode/nemotron-3-ultra-free

## Mission
This project already has most of the core work done. Now you must finish it completely by doing ALL of the following:

1. Inspect the full project state:
   - app/*.py, app/**/*.py
   - templates/index.html
   - static/js/app.js
   - static/js/api.js
   - static/css/app.css
   - tests/test_security.py
   - .opencode-report-full-audit.md
   - .opencode-review-final.md
   - .opencode-review-uiux.md

2. Identify anything still missing or incomplete from the full audit:
   - Priority workflow: visible, filterable, and actionable?
   - Assign-to-person flow: usable for agents/managers/admins?
   - AI onboarding: can a user figure out how to add their key, choose a model, and actually use the AI buttons?
   - Missing tests, missing routes, missing UI states.
   - Any broken backend/frontend integration.
   - Any missing features for daily usability.

3. Make all necessary code edits to finish the app. Do NOT just report — actually implement the missing pieces.
   - If tests are missing for new features, add them.
   - If backend routes are missing, add them.
   - If frontend UI is missing, add it.
   - If integration is broken, fix it.

4. After editing:
   - Run the test suite: `. venv/bin/activate && python -m pytest tests/test_security.py -q`
   - Verify JS syntax: `node --check static/js/app.js && node --check static/js/api.js`
   - If anything fails, fix it.

5. Commit all changes with a clear commit message.

6. Write a final report to `.opencode-report-final-pass.md` listing:
   - What was already done
   - What was missing
   - What you fixed
   - Test results
   - Anything still pending

## Constraints
- Do NOT start a server.
- Use the `impeccable` skill at MAX thoroughness for UI/UX review.
- The Flask server is already running on :5000.
- Work inside the project directory only.
- Do not push to GitHub; just commit locally.
