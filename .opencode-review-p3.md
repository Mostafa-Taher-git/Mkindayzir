# OpenCode Review Task — OpsDesk Phase 3 (SLA policies & routing)

Project root: /media/dell/New Volume/Projects/OpsDesk
The Flask app is ALREADY running on http://127.0.0.1:5000 — do NOT start your own server.

## What was added this phase
- app/db.py: added `sla_policies` (priority/category based response+resolution hours) and `ticket_sla` (per-ticket: policy link, first_response_at, breach_at, breached, response_met, resolution_met) tables; seeded default policies; `categories.default_team_id` for routing; idempotent `_migrate()` guards.
- app/routes_sla.py (NEW): helpers + endpoints:
  - pick_policy(category_id, priority), attach_sla(ticket), record_first_response(ticket_id), evaluate_on_resolve(ticket_id), _sla_summary(ticket_id)
  - GET /api/tickets/<id>/sla (any authorized viewer)
- app/routes_tickets.py: default category->team routing on create (from categories.default_team_id, fallback by access); attach SLA on create; record first response on assign/add_comment; evaluate SLA on resolve transition; SLA info added to ticket _serialize (sla dict).
- static/js/app.js + api.js: slaBadge(t) shown in ticket list rows and ticket detail header (on-track / at-risk / breached); helper.priorityPill etc.
- tests/test_security.py: Phase-3 tests (routing+SLA attach, first response + resolution eval, recently-created not breached). ALL PASS (30 total).

## Your task (DO NOT EDIT CODE — report findings only)
1. Read every file listed above (app/db.py SLA parts, app/routes_sla.py, app/routes_tickets.py create/assign/change_status/add_comment, static/js/app.js slaBadge, tests).
2. Review for: SLA correctness (breach_at math, first_response_at set exactly once, resolution_met/breached set on resolve, response_met evaluated, no double-counting), routing correctness (ticket lands in correct team), security (authz on /api/tickets/<id>/sla — requester sees only own), SQL injection (parameterized?), and frontend rendering of slaBadge in all contexts (list + detail).
3. Test live on :5000. Demo logins (password "password"): agent@opsdesk.local, sam@opsdesk.local, manager@opsdesk.local.
   - Create a ticket as sam with category_id=2 (Hardware) -> confirm team_id=IT (agent's team) and ticket_sla row exists (GET /api/tickets/<id>/sla as agent).
   - agent self-assign -> first_response_at set.
   - requester GET /api/tickets/<agent-only-id>/sla -> expect 403/404 (not leak).
4. Write full report to .opencode-report-p3.md (in this project dir): severity, file:line, evidence, recommended fix. Separate TRUE bugs from nits. Do NOT modify source files.

Report ONLY. No code edits.