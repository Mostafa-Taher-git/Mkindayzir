# OpenCode Review Task — OpsDesk v2 (AI assistance)

Project root: /media/dell/New Volume/Projects/OpsDesk
The Flask app is ALREADY running on http://127.0.0.1:5000 — do NOT start your own server.
Write the report to `.opencode-report-ai.md` inside this project. Do not edit code. Report only.

## Scope
The newly added AI assistance feature (v2, per PLAN.md §5 AI section):
- app/ai/__init__.py and app/ai/client.py — OpenRouter client (deepseek-free), prompt-injection-hardened
  system prompt, three helpers: suggest_reply, summarize_ticket, suggest_priority. Fails closed (returns None
  on any error / missing key).
- app/routes_ai.py — three GET endpoints (/api/ai/summarize/<id>, /api/ai/suggest-reply/<id>,
  /api/ai/suggest-priority/<id>), draft-only, login_required, flag-gated.
- app/routes_auth.py — /api/auth/me now returns ai_enabled.
- app/config.py — AI_ENABLED key-gated flag.
- app/__init__.py — ai blueprint registered.
- static/js/api.js — ai* methods; static/js/app.js — state.aiEnabled wired + aiPanel in ticket detail.
- tests/test_security.py — 3 AI tests (fail-closed 503, requester cannot get draft, 404 on invisible ticket).

## What to verify
1. Correctness of fail-closed behavior: without OPERADESK_OPENROUTER_KEY, config.AI_ENABLED is False and all
   three endpoints return 503 (verified live: 503). Confirm the routes cannot leak a draft or error trace.
2. Prompt-injection hardening in app/ai/client.py: is ticket/comment text truly treated as untrusted data?
   Confirm the <<<DATA>>><<<END DATA>>> delimiting + system prompt forbidding embedded instructions is sound,
   and that comment bodies have marker-like text stripped. Suggest improvements if the boundary is weak.
3. RBAC: the endpoints use @login_required only — is draft access appropriately limited? (Plan says draft-only
   for agents/managers; currently any logged-in user who can view the ticket reaches the route. Decide whether
   requesters should be blocked, and flag it.)
4. Input handling: the model response for suggest_priority is normalized to 'normal'/'urgent' server-side — is
   that robust against odd model output? Any injection via returned text rendered into the SPA?
5. Error paths: provider timeout (20s), non-200, JSON parse failure all -> 502 with clear message, no stack
   trace. Confirm no exception can escape as a 500 HTML page.
6. Frontend aiPanel: correctly gated by state.aiEnabled and staff role; renders no panel when disabled; uses
   textContent (not innerHTML) for the draft result so returned text can't XSS the SPA.
7. Run `cd "/media/dell/New Volume/Projects/OpsDesk/" && . venv/bin/activate && python -m pytest tests/test_security.py -q`
   — confirm 40 passed. Do NOT edit code.

## Deliverable
`.opencode-report-ai.md` with: verdict, TRUE bugs only (with file:line + evidence), verified-working items,
and any prompt-injection-hardening or RBAC improvements worth making. Keep it concise and honest — report only.
