# OpenCode Review Task — OpsDesk Phase 2 (Knowledge Base)

Project root: /media/dell/New Volume/Projects/OpsDesk
IMPORTANT RUNTIME NOTES (read carefully):
- The Flask server is ALREADY RUNNING on http://127.0.0.1:5000 (debug mode). Do NOT start your own server. Do NOT run run.py. Just curl the live endpoints.
- All your work MUST stay inside /media/dell/New Volume/Projects/OpsDesk. Do NOT read or write files under /tmp or any external path. Reading /tmp will be denied and aborts the task.
- Write your final review report to /media/dell/New Volume/Projects/OpsDesk/.opencode-report-p2.md (inside the project).

## What Phase 2 added (already committed; REVIEW ONLY — do not edit code)
- app/db.py: tables kb_articles (status draft|published, views) + kb_feedback.
- app/routes_kb.py: GET /api/kb (requesters see published only; ?q= search; ?category_id=),
  GET /api/kb/<id> (increments views on published), POST /api/kb (agent/manager, draft),
  PATCH /api/kb/<id> (author or agent/manager), POST /api/kb/<id>/publish (agent/manager),
  DELETE /api/kb/<id>, POST /api/kb/<id>/feedback (any logged-in user).
- static/js/api.js + static/js/app.js: Help Center (search/read/feedback), Manage KB (CRUD/publish),
  sidebar links, catSelect/debounce/categoryName helpers. static/css/app.css: KB styles.
- tests/test_security.py: test_kb_authoring_and_visibility.

## Your job (use your frontend, backend, and code-review skills)
1. Read the Phase 2 files above (all inside the project dir).
2. Hit LIVE endpoints with curl to confirm behavior (create as agent, publish, read as requester,
   search, feedback, RBAC, missing-token 403, bad payload 400).
   Demo logins (password "password"): agent@opsdesk.local, sam@opsdesk.local (requester),
   manager@opsdesk.local, admin@opsdesk.local.
   To authenticate: GET /api/auth/csrf (save cookie jar -c), POST /api/auth/login with
   header X-CSRF-Token and JSON {email,password}, then re-GET /api/auth/csrf to refresh the token.
3. Critique: correctness bugs, security gaps (authZ, XSS via unescaped body in the DOM, CSRF),
   UX/accessibility in the new JS views, maintainability.
   SPECIFICALLY CHECK: Is the KB article BODY rendered safely (no innerHTML/XSS)? Is the views
   counter incremented correctly and not abusable? Are empty title/body handled? Can a requester
   reach a draft by direct ID? Does feedback allow duplicate spam? Any broken navigation (routes)?
4. Write the report (plain text) to .opencode-report-p2.md with this format:
   - VERDICT: ship / ship-with-fixes / needs-rework
   - BUGS: file:line + one-line fix each
   - SECURITY: with severity
   - UX/A11Y: list
   - IMPROVEMENTS: optional
Do NOT modify any application files. Only write the report file. Report only.
