# OpenCode task — FULL review + fix of "Authentication required" on :5000

You are investigating a browser "Authentication required" (native Basic-Auth) dialog
that appears when opening http://localhost:5000 for the OpsDesk Flask app.

## Already proven (do NOT waste turns re-confirming with curl — it's settled)
- `curl -D - http://localhost:5000/` -> `200 OK`, **no `WWW-Authenticate` header**.
- `ss -tlnp | grep :5000` -> only `python` (the OpsDesk `python run.py`) listening.
- No proxy (GNOME proxy mode = `none`).
Conclusion: the OpsDesk SERVER is not sending an auth challenge. The dialog is
browser/client-side (cached HTTP Basic-Auth entry or an extension). No server code
change is expected.

## YOUR JOB — do the real work OpenCode is being asked for
1. **Source-level review for any Basic-Auth path.** Grep the project for every possible
   challenge source and REPORT the exact file:line for each finding (expect zero):
   - `grep -rniE "basic|www-authenticate|authorization|authenticate\\(|before_request|httpbasic|basicauth|send_authorization" app/ run.py config.py`
   - Also inspect `run.py` and any WSGI/wrapper layer for an auth middleware, and
     `requirements.txt` for any basic-auth package.
   - Read `app/__init__.py`, `app/helpers.py` (login_required), `app/routes_auth.py` to
     confirm auth is session-cookie based, not Basic.
2. **Client-side root-cause + fix.** The native dialog means the browser holds a cached
   Basic-Auth attempt for localhost:5000. Produce the precise fix the USER must run:
   - Chrome: `chrome://net-internals/#auth_cache` -> Clear; hard reload `Ctrl+Shift+R`.
   - Brave: `brave://net-internals/#auth_cache` -> Clear; hard reload.
   - If still present, incognito test to isolate extension vs wrong URL; report that logic.
3. **Deliverable:** a short report stating (a) code review result with exact lines proving
   no Basic-Auth exists, (b) the definitive client-side fix, (c) whether any OpsDesk file
   needs changing (answer: NO, unless you find a real challenge source).

Do NOT modify any OpsDesk application code. Read-only investigation + client fix guidance.
