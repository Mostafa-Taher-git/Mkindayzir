# OpenCode Review Task — OpsDesk Phase 4 (Reporting & CSAT)

Project root: /media/dell/New Volume/Projects/OpsDesk
The Flask app is ALREADY running on http://127.0.0.1:5000 — do NOT start your own server.

## What was added this phase
- app/db.py: added `csat INTEGER` column to `tickets` + idempotent `_migrate()` guard. Added `helpers._parse_iso()` shared util.
- app/routes_reports.py (NEW blueprint): reporting + CSAT endpoints:
  - GET /api/reports/summary (manager/admin) — counts, SLA attainment %, avg resolution, avg CSAT
  - GET /api/reports/workload (manager/admin) — per-agent open/resolved/avg-res
  - GET /api/reports/sla (manager/admin) — met/missed/pending
  - GET /api/reports/trend?days=N (manager/admin) — created/resolved per day
  - GET /api/reports/export.csv (manager/admin) — full ticket CSV dump
  - POST /api/tickets/<id>/rate (requester who owns + resolved/closed ticket, once) — 1-5
- app/__init__.py: registered `reports` blueprint.
- static/js/api.js: reportsSummary/Workload/Sla/Trend, exportCsv, rateTicket.
- static/js/app.js: `/reports` route + sidebar link (manager/admin), viewReports() (cards, workload table, SLA box, trend, CSV button), csatWidget() in ticket detail (requester rates resolved ticket; shows existing score).
- static/css/app.css: .report-cards, .report-value, .trend.
- tests/test_security.py: +6 Phase-4 tests (CSAT rate/forbidden, reports gating, CSV, trend validation). ALL 30 PASS.

## Your task (DO NOT EDIT CODE — report findings only)
1. Read every file listed above.
2. Review for: correctness, security (authz on every endpoint — no requester/agent leak of manager-only data; CSRF on rate), input validation, SQL correctness (no injection — confirm parameterized), frontend bugs (router, undefined refs, broken rendering of viewReports/csatWidget), and UX.
3. Test live against the running server on :5000 using curl with sessions. Demo logins (password "password"): manager@opsdesk.local (manager), agent@opsdesk.local (agent), sam@opsdesk.local (requester).
   - manager GET /api/reports/summary, /workload, /sla, /trend?days=abc, /export.csv
   - agent GET /api/reports/summary -> expect 403
   - requester POST /api/tickets/<id>/rate on a ticket they don't own -> expect 403
   - requester rate a resolved ticket twice -> second expect 400
4. Write your full report to .opencode-report-p4.md (in this project dir) with: severity, file:line, evidence, recommended fix. Separate TRUE bugs from style nits. Do NOT modify any source files.

Report ONLY. No code edits.